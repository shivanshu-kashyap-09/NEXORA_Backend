const express = require('express');
const router = express.Router();
// Import module routes here (e.g., const orderRoutes = require('../../modules/order').routes;)
// router.use('/orders', orderRoutes);
module.exports = router;
